name: Daily News Update

on:
  schedule:
    - cron: '0 8 * * *'   # Every day at 8:00 UTC (9:00 CET, 10:00 CEST)
    - cron: '0 20 * * *'  # Every day at 20:00 UTC (21:00 CET, 22:00 CEST)
  workflow_dispatch:  # Enable manual trigger

jobs:
  update-news:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          df -h
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Install CPU-only PyTorch first (saves ~3GB vs CUDA version)
          pip install --no-cache-dir torch --index-url https://download.pytorch.org/whl/cpu
          # Install remaining dependencies
          pip install --no-cache-dir -r requirements.full.txt
      
      - name: Run daily update pipeline
        env:
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
        run: |
          echo "Step 1: Fetching news..."
          python src/fetch_articles.py
          
          echo "Step 2: Scraping full content..."
          python src/scrape_articles.py
          
          echo "Step 3: Preprocessing..."
          python src/preprocess_articles.py
          
          echo "Step 4: Sentiment analysis..."
          python src/analyze_sentiment.py
          
          echo "Step 5: Topic modeling..."
          python src/discover_topics.py
          
          echo "Step 6: Summarization..."
          python src/summarize_articles.py
          
          echo "Step 7: Predictive model training..."
          python train_models.py || echo "⚠️ Predictive training skipped (may need more data)"
          
          echo "Step 8: Evaluation..."
          python src/evaluate_pipeline.py
          
          echo "✅ Pipeline complete!"
      
      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/ models/
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Daily update: $(date +'%Y-%m-%d %H:%M UTC') [skip ci]" && git push)